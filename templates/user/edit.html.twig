{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Edit User</h1>

        {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
        <button style="margin-top: 10px; height: 40px; border-radius: 4px; background-color: #5eb5e0;">
            <a href="{{ path('app_user_index') }}">Back to Profile</a>
        </button>
    </div>

{% endblock %}
